name: CI workflows
on: push
jobs:
  event-info-job:
    runs-on: ubuntu-latest
    steps: 
        - name: Print trigger Event
          run: echo "My Trigger event is ${{ github.event_name }} event"
